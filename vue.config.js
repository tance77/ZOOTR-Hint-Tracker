const fs = require('fs');
module.exports = {
    // devServer: {
    //     https: true,
        // key: fs.existsSync('localhost+3-key.pem') ? fs.readFileSync('localhost+3-key.pem') : null,
        // cert: fs.existsSync('localhost+3.pem') ? fs.readFileSync('localhost+3.pem') : null,
    // },

    css: {
        loaderOptions: {
        }
    }
};
